<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="CppGallery.Pages.UserControls.EHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CppGallery.Pages.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Margin="0,-1,0,0">

    <Grid Padding="0,10,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <FontIcon x:Name="FIcon" FontFamily="Segoe MDL2 Assets" Glyph="{x:Bind Icon}" HorizontalAlignment="Left" FontSize="24" />
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock x:Name="TitleTextBlock" />
            <TextBlock x:Name="SentenceTextBlock" Text="{x:Bind Sentence}" FontSize="12" Foreground="{ThemeResource AppBarToggleButtonCheckedPointerOverBackgroundThemeBrush}" />
        </StackPanel>
    </Grid>
</UserControl>
