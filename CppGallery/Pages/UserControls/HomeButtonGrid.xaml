<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="CppGallery.Pages.UserControls.HomeButtonGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CppGallery.Pages.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="StackPanel">
            <Setter Property="Spacing" Value="0"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal" Width="300" Height="60" Spacing="0">
        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="{x:Bind Icon}" FontSize="32"/>
        <StackPanel Margin="20,0,0,0" MaxWidth="250" Spacing="0">
            <TextBlock Text="{x:Bind Title}"/>
            <TextBlock Text="{x:Bind Sentence}" FontSize="12" Foreground="{ThemeResource AppBarToggleButtonCheckedPointerOverBackgroundThemeBrush}" Margin="0,5,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
