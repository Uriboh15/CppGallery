<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="CppGallery.Pages.UserControls.ColumnBlockPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CppGallery.Pages.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Height="60">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Style="{StaticResource EGrid}" Grid.Column="0" Margin="0,0,2,0">
            <TextBlock Text="{x:Bind Left}"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <Grid Style="{StaticResource EGrid}" Grid.Column="1" Margin="2,0,0,0">
            <TextBlock Text="{x:Bind Right}"   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
    
</UserControl>
