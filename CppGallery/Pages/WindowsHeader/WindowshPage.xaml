<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CppGallery.Pages.WindowsHeader.WindowshPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CppGallery.Pages.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer >
        <local:OuterPanel HeaderType="Windows" >
            <InfoBar Style="{StaticResource IncompleteHeaderPageInfoBar}" />
            
            <local:BlockPanel Header="一般ユーティリティ" >
                <local:ToRightButton Style="{StaticResource WinUserh}" />
            </local:BlockPanel>

            <local:BlockPanel Header="環境設定" >
                <local:ToRightButton Style="{StaticResource ProcessEnv}" />
            </local:BlockPanel>

            <local:BlockPanel Header="コンソール操作" >
                <local:ToRightButton Style="{StaticResource ConsoleApi}" />
                <local:ToRightButton Style="{StaticResource ConsoleApi2}" />
                <local:ToRightButton Style="{StaticResource ConsoleApi3}" />
            </local:BlockPanel>

            <local:BlockPanel Header="ファイル・ディレクトリ操作" >
                <local:ToRightButton Style="{StaticResource FileApih}" />
            </local:BlockPanel>
            
        </local:OuterPanel>
    </ScrollViewer>
</Page>
